<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <!-- Bootstrap CSS -->
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
        crossorigin="anonymous">

    <title>Quoting Dojo</title>

</head>

<body>
    <div class="container">

        <!-- everything that goes in container  here -->

        <h1 class="mb-2">Welcome to Quoting Dojo</h1>

        <!-- if errors returned, display them -->
        <% if(typeof(errors) != 'undefined' ) { %>
            <% for (var x in errors) { %>
                <p class="alert alert-danger">
                    <%= errors[x].message %>
                </p>
                <% } %>
                    <% } %>

                        <!-- form to collect quote data -->
                        <form action="/add_quote" method="POST">

                            <div class="form-group">
                                <label for="name">Your Name:</label>
                                <div class="col-sm-10">
                                    <input type="text" class="form-control" name="name" placeholder="Enter your name">
                                </div>
                            </div>

                            <div class="form-group">
                                <label for="quote">Your quote:</label>
                                <div class="col-sm-10">
                                    <textarea class="form-control" name="quote" placeholder="Enter your quote" rows="3"></textarea>
                                </div>
                            </div>

                            <div class="row form-group">
                                <button type="submit" class="btn btn-primary mr-3">
                                    Add my quote!
                                </button>
                                <a class="btn btn-primary" href="/quotes" role="button">Skip to quotes!</a>
                            </div>
                        </form>
    </div>
    <!-- end container div -->

</body>

</html>